{
  "name": "hass-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for Home Assistant API integration",
  "main": "dist/index.js",
  "type": "module",
  "bin": {
    "hass-mcp": "./bin/hass-mcp.js"
  },
  "scripts": {
    "build": "bunx --bun tsc",
    "start": "bun run --env-file .env src/index.ts --stdio",
    "start:mock": "bun run dist/index.js --mock",
    "start:stdio": "bun run dist/index.js --stdio",
    "start:stdio:mock": "bun run dist/index.js --stdio --mock",
    "dev": "bunx --bun tsc -w & bun --watch dist/index.js",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "ci-test": "NODE_ENV=test bun test --ci",
    "lint": "bunx --bun eslint src/**/*.ts",
    "format": "bunx --bun prettier --write src/**/*.ts",
    "hass-mcp": "bun run build && bun run start",
    "hass-mcp:mock": "bun run build && bun run start:mock",
    "inspect": "bunx --bun modelcontextprotocol/inspector bun run hass-mcp",
    "prepublishOnly": "bun run build",
    "install-global": "bun run build && bun link"
  },
  "keywords": [
    "mcp",
    "home-assistant",
    "hass",
    "smarthome",
    "iot",
    "modelcontextprotocol",
    "llm",
    "ai"
  ],
  "author": "Linus <220827+oleander@users.noreply.github.com>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/oleander/mcp-hass-server"
  },
  "homepage": "https://github.com/oleander/mcp-hass-server#readme",
  "bugs": {
    "url": "https://github.com/oleander/mcp-hass-server/issues"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.6.0",
    "dotenv": "^16.3.1",
    "hass-mcp-server": ".",
    "home-assistant-js-websocket": "^9.4.0",
    "node-fetch": "^3.3.2",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "@types/bun": "^1.2.4",
    "@types/jest": "^29.5.14",
    "@types/node": "^20.10.4",
    "@typescript-eslint/eslint-plugin": "^8.25.0",
    "@typescript-eslint/parser": "^8.25.0",
    "eslint": "^8.56.0",
    "jest": "^29.7.0",
    "prettier": "^3.1.1",
    "ts-jest": "^29.2.6",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "bun": ">=1.0.0"
  },
  "preferGlobal": true,
  "files": [
    "dist",
    "bin",
    "docs",
    "prompts",
    "README.md",
    "LICENSE"
  ],
  "private": true
}